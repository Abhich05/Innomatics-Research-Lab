import React from "react";
import ReactDOM from "react-dom";
import CountrySearch from "./App";
import "./index.css"; // Optional: for styling

ReactDOM.render(
  <React.StrictMode>
    <CountrySearch />
  </React.StrictMode>,
  document.getElementById("root")
);
